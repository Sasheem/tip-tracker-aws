import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const CalendarDetail = () => {
	return (
		<View style={{ backgroundColor: `green` }}>
			<Text>Calendar Detail View</Text>
		</View>
	);
};

export default CalendarDetail;
